export type leaderboardType = {
    userName: string,
            profilePicture: string | null,
            accuracyScore: number,
            recordId: number,
            versionId: number,
            createdAt: string,
            userAudioPath: string | null,
}