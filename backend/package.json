{
  "name": "backend",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "pull": "bunx prisma db pull && bunx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.7.0",
    "hono": "^4.7.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prisma": "^6.7.0"
  }
}
